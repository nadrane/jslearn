1. is there a reason to use react-router's <Link /> instead of <a/> ?

2. see Table constructor -- ive read that its bad to set state from props (which I was formerly doing here), but also that you don't need constructor if you're setting state statically.
